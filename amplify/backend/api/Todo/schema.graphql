type Todo
    @model
    @auth(rules: [{allow: owner}])
    @searchable
{
    id: ID!
    owner: String
    text: String!
    complete: Boolean!
    createdAt: AWSDateTime
    updatedAt: AWSDateTime
}
